[package]
name = "onehand-plugin-api"
version = "0.1.0"
edition = "2024"
description = "GUI-free identity and registration contract for built-in Onehand plugins"

[lib]
name = "onehand_plugin_api"
path = "src/lib.rs"
